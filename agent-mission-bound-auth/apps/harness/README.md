# Demo Harness

Local tutorial and testing app for Agent Mission-Bound Auth.

The harness demonstrates the protocol with a private-compute workflow, x402 payment negotiation, and Zeko receipt linkage. It is intentionally replaceable: production apps should consume the protocol package and SDK, then enforce their own domain-specific work and receipts.

Run from the repository root:

```bash
npm start
```
