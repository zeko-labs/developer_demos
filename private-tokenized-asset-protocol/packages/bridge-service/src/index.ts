export const moduleName = '@tap/bridge-service';

export function init() {
  return {
    module: moduleName,
    status: 'ready',
    note: 'Scaffold placeholder. Implement protocol logic in this package.'
  };
}
